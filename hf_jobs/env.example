# Example environment variables for HF Jobs
# Copy values into your job invocation with `-e KEY=VALUE` or set as Jobs secrets.

# WANDB
WANDB_API_KEY=your_wandb_api_key
RUN=hf-jobs-run-name

# Multi-GPU
NPROC_PER_NODE=1

# Optional git source (only needed if your CLI doesn't upload the repo)
REPO_URL=https://github.com/you/nanochatAquaRat.git
GIT_REF=main

# Optional passthrough args to training scripts
# For RL:
# RL_ARGS="--examples_per_step=16 --num_samples=16 --eval_every=120"
# For base training:
# BASE_ARGS="--depth=12 --num_iterations=2000 --eval_every=250"


