# Requirements for W&B 3D Visualization Showcase
# Install with: pip install -r requirements_3d_viz.txt

# Core dependencies (likely already installed)
torch>=2.0.0
numpy>=1.20.0
wandb>=0.69.0

# Visualization dependencies (NEW - required for 3D viz)
plotly>=5.18.0
scipy>=1.10.0

# Optional for embedding visualizations
scikit-learn>=1.3.0  # For PCA and t-SNE
umap-learn>=0.5.0    # For UMAP (optional, falls back to PCA if not available)

# Development/testing
jupyter>=1.0.0       # For notebook examples (optional)
